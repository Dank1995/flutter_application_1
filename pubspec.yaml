name: cadence_coach
description: A cycling cadence coach with efficiency optimizer and visual alerts
version: 61.0.0+61  # Version 61

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^2.0.2   # valid stable version
  path_provider: ^2.0.15
  csv: ^5.0.0
  fl_chart: ^0.60.0

dev_dependencies:
  flutter_test:
    sdk: flutter
