name: physiological_optimiser
description: Physiological Optimiser workout app with cadence optimizer
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter
